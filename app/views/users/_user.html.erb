<div class="user">
 <img src="<%=current_user.photo%>" class="user-image" />
  <a class="link-to-user" href="/users/<%= user.id %>"><%= user.name %></a>
  <h4 class="posts-count">Posts(<%= user.posts.size %>)</h4>
  <%= link_to "admin settings", edit_user_path(user.id) if can? :update, user   %>
</div>